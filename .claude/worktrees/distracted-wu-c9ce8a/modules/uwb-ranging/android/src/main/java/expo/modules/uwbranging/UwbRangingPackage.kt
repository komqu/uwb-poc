package expo.modules.uwbranging

/**
 * Modern Expo modules are registered through `expo-module.config.json`.
 * This file is kept as a marker/anchor for the package so older tooling
 * (and humans grepping the project) can find the module on disk.
 *
 * The actual module class is `UwbRangingModule`, registered in
 * `expo-module.config.json` -> android.modules.
 */
class UwbRangingPackage
